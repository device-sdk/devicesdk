---
"@devicesdk/docs": patch
---

Fix the changelog's firmware distribution description: firmware ships as versioned GitHub Releases that the server fetches at boot, not bundled into the Docker image.
