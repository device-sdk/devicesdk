---
description: Checkout main and pull the latest changes from origin.
---

Run `git checkout main && git pull` in the current working directory.
Report the result concisely, including whether the branch was already on main,
whether a fast-forward occurred, and any merge conflicts that require attention.
