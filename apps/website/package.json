{
  "name": "@devicesdk/website",
  "version": "0.0.0",
  "description": "",
  "license": "SEE LICENSE IN LICENSE",
  "author": "",
  "type": "module",
  "private": true,
  "scripts": {
    "build-tailwind": "npx @tailwindcss/cli -i ./src/input.css -o ./static/styles.css",
    "build": "rm -rf public && mkdir -p static/api/docs && cp ../api/openapi.json static/api/docs/openapi.json && pnpm run build-tailwind && node generate-og.js && node generate-agent-skills.js && hugo",
    "dev": "hugo server -D",
    "deploy": "npx wrangler deploy",
    "clean": "rm -rf public resources/_gen"
  },
  "dependencies": {
    "wrangler": "catalog:"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "glob": "^13.0.0",
    "gray-matter": "^4.0.3",
    "playwright": "^1.57.0",
    "tailwindcss": "catalog:tailwind4"
  }
}
