---
"@devicesdk/api": patch
---

Add integration tests for the firmware download endpoint covering 401, 404 (project/device/firmware not found), happy path response headers, and managed token creation
