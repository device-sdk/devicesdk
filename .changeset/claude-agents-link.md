---
"@devicesdk/server": patch
---

Create `CLAUDE.md` as a symlink to `AGENTS.md` so Claude Code automatically loads the project guidance, and update the Agent Configuration section to document the link.
