{
	"name": "devicesdk-monorepo",
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"dev": "turbo run dev",
		"lint": "biome check --write apps/api packages/core packages/cli && turbo run lint",
		"test": "turbo run test",
		"check-types": "turbo run check-types",
		"deploy": "turbo run deploy",
		"clean": "turbo run clean"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.8",
		"turbo": "^2.5.0"
	},
	"packageManager": "pnpm@9.15.4",
	"engines": {
		"node": ">=20"
	}
}
