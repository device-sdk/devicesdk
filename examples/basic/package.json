{
  "name": "@devicesdk/example-basic",
  "private": true,
  "version": "0.1.10",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "devicesdk dev",
    "build": "devicesdk build",
    "deploy": "devicesdk deploy",
    "local:login": "DEVICESDK_API_URL=http://localhost:8080 npx devicesdk login",
    "flash-remote": "devicesdk flash device",
    "flash-local": "DEVICESDK_API_URL=http://localhost:8080 devicesdk flash device --host ${DEVICESDK_DEVICE_HOST:-localhost:8080}"
  },
  "dependencies": {
    "@devicesdk/core": "workspace:*"
  },
  "devDependencies": {
    "@devicesdk/cli": "workspace:*",
    "typescript": "catalog:"
  }
}
