{
  "name": "@devicesdk/api",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "schema": "npx chanfana",
    "lint": "npx @biomejs/biome format --write src && npx @biomejs/biome lint --write src",
    "test": "tsc && wrangler deploy --dry-run && npx vitest run --config tests/vitest.config.mts",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@devicesdk/core": "workspace:*",
    "@hono/oauth-providers": "^0.8.5",
    "@sentry/cloudflare": "^10.29.0",
    "chanfana": "2.8.3",
    "hono": "4.10.7",
    "workers-qb": "^1.11.2",
    "zod": "catalog:zod3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@cloudflare/vitest-pool-workers": "0.11.1",
    "@cloudflare/workers-types": "^4.20251225.0",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
