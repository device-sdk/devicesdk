---
"@devicesdk/dashboard": minor
---

The device details page now shows the firmware version and device type a device is running (from the new firmware-version handshake), with an `Unknown` fallback for legacy firmware that does not report a version.
