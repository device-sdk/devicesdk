---
"@devicesdk/api": minor
---

Add CLI token list and revoke endpoints (GET /v1/tokens/cli, DELETE /v1/tokens/cli/:tokenId) and display CLI sessions in the dashboard tokens page with revoke support.
