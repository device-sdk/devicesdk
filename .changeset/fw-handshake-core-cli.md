---
"@devicesdk/core": minor
"@devicesdk/cli": minor
---

Add a shared `DeviceType` union and firmware/device-type reporting to `devicesdk status`: the status table now shows the FIRMWARE column (firmware version and device type reported by the device) and JSON output gains `firmwareVersion` / `deviceType`.
