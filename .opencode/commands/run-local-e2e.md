---
description: Run the full DeviceSDK stack end-to-end with a real ESP32 on the LAN.
---

Follow `.opencode/skills/run-local-e2e/SKILL.md` to run the full DeviceSDK stack
end-to-end for $ARGUMENTS. Discover the LAN IP and device port, start the local
server and dashboard, configure the host firewall, authenticate, deploy a user
script, build and flash the ESP32 firmware from source, and verify the device
connects.
