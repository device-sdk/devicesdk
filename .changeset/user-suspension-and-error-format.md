---
"@devicesdk/api": minor
---

Add user suspension mechanism (suspended_at column, 403 response on all auth paths) and standardize API error responses to use singular `error` key instead of `errors` array.
