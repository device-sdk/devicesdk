---
"@devicesdk/website": patch
---

The `devicesdk-api` agent skill now points at the self-hosted interactive
OpenAPI reference (`/api-docs` on your own server) instead of a dead
`https://devicesdk.com/docs/api` URL.
