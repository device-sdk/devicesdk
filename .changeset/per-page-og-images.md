---
"@devicesdk/website": patch
---

Add per-page Open Graph `social_image` front-matter to the docs. `generate-og.js` now points `concepts/identifiers` at its own card instead of the shared `concepts.png`, and the five `errors/*` pages each get a per-page social image.
